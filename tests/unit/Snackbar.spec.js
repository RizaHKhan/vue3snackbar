import { shallowMount } from "@vue/test-utils";
import Snackbar from "@/components/Snackbar.vue";

describe("Snackbar.vue", () => {
  it("should be hidden if no messages in store array", () => {});
  it("should show message for a few seconds and then should disappear", () => {});
  it("should show the next message in the array after a few seconds", () => {});
});
